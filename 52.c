#include <stdio.h>
#include <string.h>
int main()
{
    char s1[] = "mudit";
    char s2[] = "sharma";
    printf("the reversed string s1 is:");
    puts(strrev(s1));
    printf("the reversed string s2 is:");
    puts(strrev(s2));
    return 0;
}